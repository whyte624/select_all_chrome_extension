{
  "manifest_version": 2,
  "name": "Select all (text)",
  "description": "This extension allows you to access to Select all function (Ctrl+A) in the context menu after mouse right click",
  "version": "0.2",
  "permissions": [
    "contextMenus",
    "activeTab"
  ],
  "background": {
    "scripts": [
      "background.js"
    ]
  }
}